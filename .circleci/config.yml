version: 2.1

jobs:

  print-world:
    docker:
      - image: circleci/node:13.8.0
    steps:
      - checkout
      - exit 1
      - run:
          name: on error
          command: echo "Hello Error!"
          when: on_fail


workflows:
  say-hello-workflow:
    jobs:
      - print-world

